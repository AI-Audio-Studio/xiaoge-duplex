"""小歌自有代码的公共层(阶段1)。

- text_rules    : 停止词/附和/压话确认/数字归一化 的正则与判定(纯文本逻辑)
- config_utils  : 统一的 env 解析 helper(收敛原先散落 5 处的 _env_*/_parse_*)
- runtime       : UTF-8 stdio、毫秒格式化、turn 指标日志落盘
- taps          : 音频旁路 tap 基类(输入透传观察 / 输出转发观察)

只放"被 ≥2 个模块共用、且自身零工程依赖"的代码;应用编排/状态不进这里。
"""
